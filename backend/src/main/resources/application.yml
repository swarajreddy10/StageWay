server:
  port: ${PORT:8081}
  tomcat:
    max-swallow-size: 20MB
    max-http-form-post-size: 20MB
  max-http-header-size: 20MB

spring:
  application:
    name: event-management-backend
  config:
    import: optional:file:.env.properties,optional:file:backend/.env.properties
  main:
    banner-mode: "off"
  
  # File Upload Configuration
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
      file-size-threshold: 2MB
  
  # Database Configuration
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 60000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  # JPA Configuration - Let Flyway handle schema
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  # Flyway Configuration
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    clean-disabled: ${FLYWAY_CLEAN_DISABLED:true}
  
  # Redis Configuration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
    repositories:
      enabled: false
  
  # Session Configuration
  session:
    store-type: redis
    timeout: 3600s
  
# Application Configuration
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000,http://192.168.*:3000,http://10.*:3000,http://172.16.*:3000}

  security:
    allow-local-tokens: ${ALLOW_LOCAL_TOKENS:false}
    session-cookie-name: ${SESSION_COOKIE_NAME:stageway.session}
    oauth-state-cookie-name: ${OAUTH_STATE_COOKIE_NAME:stageway.oauth}
    secure-cookies: ${SECURE_COOKIES:false}
  
  qr:
    secret: ${QR_SECRET:myQRSecretKeyThatIsAtLeast32CharactersLong}
    ttl: PT48H

# Supabase Configuration
supabase:
  url: ${SUPABASE_URL:}
  anon-key: ${SUPABASE_ANON_KEY:}

# Logging Configuration
logging:
  level:
    root: INFO
    com.eventmanagement: INFO
    org.springframework: WARN
    org.springframework.boot: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN
    org.springframework.data.redis: WARN
    org.hibernate: WARN
    org.flywaydb: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{requestId}] - %msg%n"

# Management Endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
